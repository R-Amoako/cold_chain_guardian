[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cold-chain-guardian"
version = "0.1.0"
description = "Cold chain monitoring prototype"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["data_generator"]